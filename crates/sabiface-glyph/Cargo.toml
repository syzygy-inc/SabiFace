[package]
name = "sabiface-glyph"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Glyph outline sources for TeX fonts: Type1 charstrings, CFF/TrueType via ttf-parser"

[dependencies]
ttf-parser = { workspace = true }

[dev-dependencies]
sabiface-metrics = { path = "../sabiface-metrics" }
